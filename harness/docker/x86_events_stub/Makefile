# GGCC_SKIP_EVENTS — freestanding C1 x86_64: soft cannot compile events/core.c
# ("not an aggregate"). arch/x86 Kconfig selects PERF_EVENTS (cannot Kconfig-off).
# PVH → busybox ring3 path does not need perf events subsystem.
obj-y :=
obj-m :=
